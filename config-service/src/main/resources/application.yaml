server:
  port: 8001
spring:
  cloud:
    config:
      server:
        git:
          default-label: zipkin-sleuth
          uri: https://github.com/josepweb/microservicios-tutorial
          search-paths: config-data
  application:
    name: config-service